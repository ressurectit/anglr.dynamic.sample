import {LayoutComponentMetadata} from '@anglr/dynamic/layout';

/**
 * Options for sample root block component
 */
export interface SampleRootBlockComponentOptions
{
    /**
     * Metadata for component displayed as content of sample root block
     */
    content: LayoutComponentMetadata|undefined|null;
}